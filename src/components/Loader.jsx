import React from "react";
import "./Loader.css"; // Make sure to create a corresponding CSS file for styling

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
